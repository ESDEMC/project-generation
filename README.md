# Project Generation

Project Generation compiles declarative JSON/YAML definitions and customer source data into a validated generated project. The default infrastructure writer produces Latch-Up project files.

See the [documentation](docs/README.md), [runnable examples](examples/README.md), and [changelog](CHANGELOG.md).
